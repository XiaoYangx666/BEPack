{
  "name": "mcbe-bepack-addon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bepack:install": "bepack install",
    "dev": "bepack dev --copy",
    "build": "bepack build",
    "pack": "bepack pack",
    "release": "bepack build --install --pack"
  },
  "devDependencies": {
    "@bepack/cli": "^0.1.0-beta.0",
    "typescript": "7.0.2"
  }
}
